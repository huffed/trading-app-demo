# E2.24.d — Final re-derivation on the complete fidelity harness (2026-07-17)

**Harness:** pinned XAU/USD H4 (17,810 bars, sha `6d10d04b…`) with ALL four fidelity corrections active:
floating-equity ML (d.i) + de-compounding to window-start capital (d.ii) + gap-open exit fills (d.iv) + NY-session daily boundary (E2.25.b). Sibling-aware, all seven Phase-B fidelity gates on. `MODE=rederive`.

## Results (ML-equalized to worst-window ML = 8.0)

| Arm | risk | worst ML | worst DL | breaches | avgRet/window | @ML=8 risk | @ML=8 return | P(pass) | median |
|---|---|---|---|---|---|---|---|---|---|
| **4-algo (deployed)** | 0.60% | **10.08%** | 4.77% | **1** | 0.95% | **0.476%** | **0.38%/mo** | 80.6% | 9.0mo |
| 5-algo +BOS rr25_lb3 | 0.60% | 10.00% | **6.20%** | **8** | 1.31% | 0.480% | 0.52%/mo | 81.9% | 5.7mo |
| **5-algo +Engulfing rr25_lb4** | 0.60% | 11.50% | 4.77% | 2 | 1.58% | **0.417%** | **0.55%/mo** | 80.1% | 6.0mo |

**ML-equalized winner: 5-algo +Engulfing — 0.55%/mo at 0.417% risk.**

## What changed vs the prior (optimistic) numbers

The 2026-07-17 pre-fidelity figure was **1.92%/challenge ≈ 0.96%/mo, worst ML 9.03%** (4-algo @0.66%, realized-only stressTest, compounding-inflated). The complete harness reveals two independent optimism sources:

1. **De-compounding (d.ii):** trades were sized on compounding equity (late-sample ~1.8× the fresh-$10k a challenge starts with); expressing returns on entry-equity roughly **halves** the apparent per-window return.
2. **Floating-equity + gap fills (d.i/d.iv):** raise the 4-algo worst ML **9.03% → 10.08%** — it now BREACHES the 10% FTMO static limit in 1/576 windows at 0.60%, forcing smaller risk to stay safe.

Net: at the operator's own ML≤8 sizing rule, the deployed 4-algo does **~0.38%/mo**, the best 5-algo arm **~0.55%/mo** — both well below the 1%/mo win condition.

## Caveats (both directions)

- The floating-ML model is **conservative** (assumes all concurrent open positions sit at their MAE simultaneously) → it UPPER-bounds ML. True ML is between realized-only (9.03%) and this (10.08%), so the true ML≤8 risk is likely ~0.48–0.52% and the true return slightly above 0.38%/mo.
- Linear ML-equalization (risk ∝ ML ∝ return under RPT) is first-order; the exact per-arm re-run at the equalized risk is the confirmation step before any deploy.
- Correlation still measured by exit-day Pearson in the sibling-aware run (E2.24.f.v: understates; mark-to-market ρ is the upgrade). The +Engulfing shares a pattern family with a deployed member.

## S3 finding confirmed (E2.25.j)

The prior "additions rejected, greedy stops at 4" was a fixed-0.60%-risk-cap artifact. At **equal tail risk**, both 5-algo arms BEAT the 4-algo (0.52–0.55 vs 0.38%/mo). The +BOS arm breaches DL (6.20%) at 0.60% — a real disqualifier; +Engulfing is the cleaner winner but carries higher pre-rescale ML (11.50%) and pattern-family overlap. **Portfolio composition (4 vs 5 algo) is now a live, evidence-backed operator decision**, not a closed question.

## Strategic implication

Fully-fidelity-corrected, deterministic gold does **0.38–0.55%/mo at safe sizing** — roughly half the already-corrected 0.96%/mo, and below every return floor in play. This is the empirical case, now on verdict-grade data with a complete harness, that the deterministic path alone cannot reach the operator's targets — the quantitative trigger for E2.26 (LLM-trader un-veto) and/or an explicit target revision.
