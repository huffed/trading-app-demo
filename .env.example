# Supabase
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Groq (free tier — Llama 3.3 70B)
GROQ_API_KEY=your-groq-api-key

# Alpha Vantage (free tier — 25 requests/day)
ALPHA_VANTAGE_API_KEY=your-alpha-vantage-api-key

# Finnhub (free tier — 60 calls/minute)
FINNHUB_API_KEY=your-finnhub-api-key

# Twelve Data (free tier — 800 credits/day, primary price data)
TWELVE_DATA_API_KEY=your-twelve-data-api-key

# Service-role key for cron jobs (Supabase project settings → API → service_role).
# NEVER prefix with NEXT_PUBLIC_ — server-only.
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Shared secret for the /api/cron/* routes. Generate with `openssl rand -hex 32`.
# Set the same value in Vercel project settings so Vercel Cron can invoke the route.
CRON_SECRET=your-cron-secret
